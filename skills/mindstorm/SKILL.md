---
name: mindstorm
description: Use when the user drops a raw idea, spark, or "what if" that deserves its own note but isn't a committed project yet — "/mindstorm", "idea:", "random thought".
---

# Mindstorm

Capture a spark as an atomic idea note, linked into the vault.

## Steps

1. **Note**: `3-Resources/<Short Idea Title>.md` from `Templates/Mindstorm`:
   - Spark: what triggered it (user's words)
   - Core concept: 2-5 sentences, sharpened
   - Open questions: at least 2, including ones the user didn't raise
   - Related: wikilinks to projects/notes it touches
2. **If it belongs to an existing project** (mentions it, or obviously extends it): put it in that project's folder instead, and add a line to the project's changelog/status log.
3. **Backlink**: today's daily note `## Mindstorm / Ideas` gets a one-liner + wikilink (create today's note only if it already should exist — don't force).
4. **Commit**: `Mindstorm: <short title>`.

## Rules

- One idea per note. Two ideas in one message → two notes.
- Title = noun phrase someone would search for later, not a sentence.
- Never judge/kill the idea at capture; open questions carry the skepticism.

## Common mistakes

- Filing project-bound ideas in 3-Resources — check `1-Projects/` first.
- Titles like "Idea 2026-07-13" — undiscoverable later.
